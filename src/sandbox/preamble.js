/**
 * The fixed, read-only JSX preamble shown above the student editor.
 * Mirrors api/starter_templates.php's starter_preamble() so the PHP
 * seed text and this display copy never drift apart; if you edit one,
 * edit the other.
 */
export const PREAMBLE = `/**
 * DataDisplay.jsx
 * Your component. Fetch FPL data with axios and render it below.
 */
import React, { useState, useEffect } from 'react';
import axios from 'axios';
import config from './config';

`;
